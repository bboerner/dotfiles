# grc overides for ...
if [ -x /usr/bin/grc ]; then
    alias gls="grc --colour=auto ls -F"
    alias gl="grc --colour=auto ls -lAh"
    alias ll="grc --colour=auto ls -l"
    alias la="grc --colour=auto ls -A"
fi
